def longest_word(words_list):
    longest_word = max(words_list, key=len)
    return longest_word, len(longest_word)

# Example usage:
words = ["Exercises", "are", "fun"]
print(longest_word(words))  # Output: ('Exercises', 9)
