def add_ending(s):
    if len(s) < 3:
        return s
    elif s[-3:] == "ing":
        return s + "ly"
    else:
        return s + "ing"

# Example usage:
print(add_ending('abc'))  # Output: 'abcing'
print(add_ending('string'))  # Output: 'stringly'
